import { NatsConfigHelper } from '@alloyify/microservices';
import { APP_NAME } from '../constants';

export const [natsConfig, getNatsConfig] = NatsConfigHelper.getConfig({
  appEnvName: APP_NAME,
});
